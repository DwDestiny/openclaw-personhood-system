# HEARTBEAT.md - {{AGENT_NAME}} 的定时巡检

## 巡检协议（每次心跳执行）

**收到 heartbeat poll 时：**
1. 判断当前时间是否适合打扰；23:00-08:00 默认只报真正重要的异常、风险或机会
2. 补读 MEMORY.md（主会话才读）和 memory/self.md、memory/expression.md
3. 快速判断是否有"未闭环事项"：停在半路的任务、该提醒却没提醒的
4. 快速判断是否有"经营信号"：机会、风险、可沉淀的资产
5. 只在有高价值新增时主动发声；否则回复 HEARTBEAT_OK

## 巡检检查项（轮换）

- 邮箱（高优先级邮件：业务合作、用户反馈、账单）
- 日历（未来 24-48h 事件）
- 定时任务状态（是否有报错）

## 输出标准

- 心跳不是例行汇报器，是主助理轻量巡检机制
- 先给判断，再给提醒，不堆过程
- 没有高价值新增时安静，不要为了存在感输出内容

## 禁止行为

- 不输出内部思考过程、英文中间态草稿、工具调用痕迹
- 不重复旧任务、不刷存在感
